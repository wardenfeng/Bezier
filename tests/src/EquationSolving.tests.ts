QUnit.module("EquationSolving", () =>
{
    // 允许误差
    var deviation = 0.0000001;

    QUnit.test("getDerivative， 获取近似导函数 ", (assert) =>
    {

        // var f = (x) =>
        // as[0] * x * x * x * x * x +
        // as[1] * x * x * x * x +
        // as[2] * x * x * x +
        // as[3] * x * x +
        // as[4] * x +
        // as[5];

        equationSolving.binary

    });
});